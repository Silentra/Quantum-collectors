# Physical-print tall frames (canonical)

Approved MPC-calibrated frame masters for **print-export only**.

- Size: **816 × 1152** (+42px art-rail cut-and-extend from original 816 × 1110)
- Format: PNG (pixel-exact soft alpha)
- Used with `compositionScale = 0.95` on an 816 × 1110 canvas

Original production frames remain at `assets/card-frames-export-cards/frame-*.webp` and
`normalized/` — do not overwrite those. These tall PNGs are print-path only.

Surgery provenance: see `../test-derivatives/_tall-frames-manifest.json`.
